\section{Methodology}
\label{sec:method}

\subsection{Overview of the Proposed Approach}

The method builds directly on the baseline CBMC architecture described in \Cref{sec:background}
and summarized in \Cref{fig:baseline-cbmc}. The goal is not to replace bounded model
checking, symbolic execution, or the solver stage. The goal is to construct a more accurate
checking model for OSEK/VDX applications before final verification. For ordinary C programs,
the baseline CBMC flow is often sufficient. For OSEK/VDX applications, however, the
checking model must also reflect how the deterministic scheduler, the ready queue, and the
standardized application interfaces (APIs) determine task execution order.

This section explains the proposed approach in four parts. First, it explains how the
checking model takes the OSEK/VDX scheduler and APIs into account. Second, it explains how
the checking model avoids a large number of unnecessary interleavings. Third, it explains
how those two ideas are combined to construct the final checking model. 

Table~\ref{tab:responsibilities} provides a compact comparison between baseline CBMC and the
proposed approach. Its purpose is to bridge the baseline architecture in Section~2 and the
methodology developed in the rest of this section.

\begin{table}[H]
  \centering
  \small
  \begin{tabularx}{\linewidth}{@{}>{\raggedright\arraybackslash}p{0.18\linewidth}>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}X@{}}
    \toprule
    Aspect & Baseline CBMC & Proposed Approach \\
    \midrule
    Task execution order & Follows ordinary program flow unless scheduling behaviour is
    explicitly encoded elsewhere. & Follows task states, the ready queue, and the
    deterministic scheduler inside the checking model. \\
    Standardized APIs & Can be treated as ordinary function effects if no OSEK/VDX-specific
    model is added. & Are interpreted as operations that may change task states, events, the
    ready queue, and therefore the next running task. \\
    Interruption-related behaviour & Broad asynchronous modelling can expose many possible
    interference points in the checking model. & Interruption-related behaviour is introduced
    only at program points where it can influence the checked execution. \\
    Unnecessary interleavings & Irrelevant interleavings may remain in the checking model and
    enlarge the verification problem. & Unnecessary interleavings are reduced before final
    bounded model checking. \\
    Expected accuracy outcome & May report a spurious bug or miss scheduler-dependent task
    execution order. & Aims to represent OSEK/VDX executions more faithfully while keeping
    CBMC's bounded model checking structure. \\
    \bottomrule
  \end{tabularx}
  \caption{Conceptual comparison between baseline CBMC and the proposed checking approach.}
  \label{tab:responsibilities}
\end{table}

The key point in Table~\ref{tab:responsibilities} is that the proposed approach does not
replace CBMC with a different verifier. It changes how the checking model is constructed
before final verification so that the solver is applied to a model that better reflects the
executions of OSEK/VDX applications.

\subsection{Taking the OSEK/VDX Scheduler into Account}

The first methodological step is to represent the behaviour of the OSEK/VDX scheduler inside
the checking model. In an OSEK/VDX application, the running task is not chosen by an
unstructured interleaving of threads. It is chosen by a deterministic scheduler that depends
on task states, priorities, events, and the ready queue. If the checking model ignores those
elements, the resulting task execution order no longer matches the execution order of the
application.

Consider a small synthetic example used throughout this section. \textit{TaskControl} has
higher priority and waits for new sensor data. \textit{TaskLogger} can run while no sensor
data are available. \textit{ISR\_Sensor} writes the shared state that says the sensor data are
ready and also signals the event expected by \textit{TaskControl}. This example is small, but
it contains the essential OSEK/VDX behaviours: waiting, readiness, dispatch, and an
interruption that changes which task may run next.

The example shows why standardized application interfaces (APIs) cannot be treated as
ordinary C-function effects. When \textit{TaskControl} waits for data, the important effect is
not merely that a call returns or stores a local value. The important effect is that the task
may move from the running state to the waiting state. Once that happens, \textit{TaskLogger}
may become the next runnable task. Later, when \textit{ISR\_Sensor} signals the event and
makes \textit{TaskControl} ready again, the deterministic scheduler may dispatch
\textit{TaskControl} instead of leaving \textit{TaskLogger} in control. The checking model
must therefore represent state changes in the scheduler domain, not just ordinary
control-flow changes in the current function.

Figure~\ref{fig:osek-sequence} summarizes this idea as a cycle. The figure is not meant to
show source-code structure. It shows the conceptual path by which a standardized API affects
task states and events, changes the ready queue, influences the deterministic scheduler, and
thereby determines the next task execution step in the checking model.

\begin{figure}[t]
  \centering
  \input{figures/osek-dispatch-sequence}
  \caption{Conceptual cycle from standardized application interfaces (APIs) to task states,
  the ready queue, the deterministic scheduler, and the next task execution step.}
  \label{fig:osek-sequence}
\end{figure}

Read from left to right, the cycle says the following. A standardized API can change task
states or events. Those changes alter the contents of the ready queue. The ready queue is then
used by the deterministic scheduler to determine task execution order. That task execution
order determines the next step represented in the checking model, which in turn leads to the
next API call or state update. Baseline CBMC follows ordinary control-flow effects, whereas
the proposed approach must also update task state and scheduler state in the checking model.

In the running example, this means that the verification path should not simply continue with
the syntactically next statement inside \textit{TaskControl} after the wait. Instead, the
checking model should represent that \textit{TaskControl} is blocked, that
\textit{TaskLogger} may run while the higher-priority task is waiting, and that
\textit{ISR\_Sensor} can make \textit{TaskControl} ready again. Only then does the task
execution order in the verification result reflect the execution order defined by the
OSEK/VDX scheduler.

\subsection{Reducing Unnecessary Interleavings}

Taking the scheduler into account is not sufficient by itself. The second methodological step
is to reduce unnecessary interleavings in the checking model. Existing checking approaches for
concurrent software often consider a very broad set of asynchronous behaviours. In the present
setting, that can enlarge the verification problem and can also produce a spurious bug when
interruption-related behaviour is introduced at program points that are irrelevant to the
property being checked.

The key question is therefore not ``where could an interruption happen in general?'' The key
question is ``where can interruption-written state affect the main verification path?'' This is
a narrower and more useful question because it focuses the checking model on observable
effects. If interruption-written state cannot influence a later branch, task release, or
property-relevant computation, then representing interruption-related behaviour at that point
adds interleavings without improving accuracy.

The running example makes this distinction concrete. Suppose \textit{ISR\_Sensor} only changes
the sensor-ready state and the event awaited by \textit{TaskControl}. Then interruption-related
behaviour matters near the points where the application tests whether data are ready, waits
for the event, or resumes after that event becomes available. By contrast, if
\textit{TaskLogger} is executing a local formatting or checksum computation that neither reads
the sensor-ready state nor influences a later decision that depends on it, adding
interruption-related behaviour there expands the checking model without changing the execution
that matters for the property.

This distinction is important for both accuracy and cost. If irrelevant program points are
included, the checking model may admit behaviours that the application cannot actually observe
through the scheduler-relevant state. Those extra behaviours consume time and memory, and they
can create counterexamples whose root cause is the modelling choice rather than the target
application. Reducing unnecessary interleavings therefore serves two purposes at once: it
keeps the checking model smaller, and it makes the verification result more faithful.

\subsection{Building the Final Checking Model}

The proposed approach combines the previous two ideas by separating two decisions that are
often mixed together. The first decision is analytical: identify the program points at which
interruption-related behaviour can influence the checked execution. The second decision is
constructive: build a new checking model that introduces interruption-related behaviour only
at those selected points.

This separation matters because the two decisions answer different questions. The analytical
step asks where interruption-written state can affect task execution order or a
property-relevant computation. The constructive step asks how to represent those effects in
the final checking model. Keeping those steps separate avoids a monolithic transformation in
which relevance and model rebuilding are entangled. As a result, the final checking model can
remain narrower than a blanket insertion of interruption-related behaviour at many unrelated
points.

In the running example, the analytical step would keep locations near the event wait, the
sensor-ready decision, or the resumption of \textit{TaskControl}, because those locations can
observe the state written by \textit{ISR\_Sensor}. It would exclude unrelated local
computations inside \textit{TaskLogger} that cannot observe that state. The constructive step
would then build a new checking model in which interruption-related behaviour is represented
only near the retained locations. In that way, the final checking model preserves the main
advantage claimed in Sections~1 and~2: it reduces unnecessary interleavings without ignoring
the OSEK/VDX scheduler.

\begin{figure}[t]
  \centering
  \input{figures/improved-pipeline-flow}
  \caption{Conceptual two-stage construction of the final checking model.}
  \label{fig:improved-pipeline}
\end{figure}

Figure~\ref{fig:improved-pipeline} illustrates this two-stage construction. From left to
right, the figure starts with the original source program and configuration data, constructs
an initial checking model, analyzes where interruption-related behaviour matters, records the
selected program locations, rebuilds a restricted checking model, and finally applies bounded
model checking to that narrower model. The figure is therefore a conceptual flow of model
construction, not a listing of implementation modules.
