\begin{tikzpicture}[
  node distance=8mm and 8mm,
  box/.style={
    draw,
    rounded corners=2pt,
    align=center,
    text width=31mm,
    minimum height=10mm,
    fill=gray!8
  },
  arrow/.style={-{Latex[length=2mm]},thick}
]
  \node[box] (src) {C sources\\and harness};
  \node[box,right=of src] (goto) {Front-end\\and \texttt{goto-cc}};
  \node[box,right=of goto] (model) {GOTO program};
  \node[box,below=of model] (symex) {Symbolic\\execution};
  \node[box,left=of symex] (solver) {SAT/SMT\\solver};
  \node[box,left=of solver] (trace) {Verification result\\or counterexample};

  \draw[arrow] (src) -- (goto);
  \draw[arrow] (goto) -- (model);
  \draw[arrow] (model) -- (symex);
  \draw[arrow] (symex) -- (solver);
  \draw[arrow] (solver) -- (trace);
\end{tikzpicture}
