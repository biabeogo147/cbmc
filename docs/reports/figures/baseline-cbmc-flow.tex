\begin{tikzpicture}[
  >=Latex,
  font=\scriptsize,
  node distance=5mm,
  box/.style={
    draw,
    rounded corners=2pt,
    align=center,
    text width=30mm,
    minimum height=10mm,
    inner sep=3pt,
    fill=gray!8
  },
  arrow/.style={->,thick}
]
  \node[box] (src) {C source\\program};
  \node[box,right=of src] (goto) {Intermediate model\\via \texttt{goto-cc}};
  \node[box,right=of goto] (symex) {Symbolic execution\\and SSA equation};
  \node[box,right=of symex] (solver) {SAT/SMT solving\\and verdict};

  \draw[arrow] (src) -- (goto);
  \draw[arrow] (goto) -- (symex);
  \draw[arrow] (symex) -- (solver);
\end{tikzpicture}
