\begin{tikzpicture}[
  node distance=7mm,
  box/.style={
    draw,
    rounded corners=2pt,
    align=center,
    text width=42mm,
    minimum height=10mm,
    fill=orange!6
  },
  arrow/.style={-{Latex[length=2mm]},thick}
]
  \node[box] (cli) {\texttt{cbmc}\\option parsing};
  \node[box,below=of cli] (symex) {\texttt{goto-symex}\\call hook};
  \node[box,below=of symex] (dispatch) {\texttt{os\_api\_dispatcher}};
  \node[box,below=of dispatch] (backend) {OSEK backend\\classification and policy};
  \node[box,below=of backend] (state) {\texttt{scheduler\_state}};

  \draw[arrow] (cli) -- node[right,align=center]{\texttt{--os-api osek}\\\texttt{--osek-oil}} (symex);
  \draw[arrow] (symex) -- node[right]{recognized call} (dispatch);
  \draw[arrow] (dispatch) -- node[right]{API kind + args} (backend);
  \draw[arrow] (backend) -- node[right,align=center]{queue, event,\\task updates} (state);
  \draw[arrow] (state.east) -| ++(18mm,0) |- node[right,align=center]{\texttt{next\_step\_kindt}\\optional writeback} (symex.east);
\end{tikzpicture}
