\begin{tikzpicture}[
  node distance=8mm and 10mm,
  box/.style={
    draw,
    rounded corners=2pt,
    align=center,
    text width=35mm,
    minimum height=10mm,
    fill=orange!6
  },
  arrow/.style={-{Latex[length=2mm]},thick}
]
  \node[box] (api) {Standardized\\application\\interfaces (APIs)};
  \node[box,right=of api] (state) {Task states\\and events};
  \node[box,right=of state] (queue) {Ready queue};
  \node[box,below=of queue] (sched) {Deterministic\\scheduler};
  \node[box,left=of sched] (order) {Task execution\\order};
  \node[box,left=of order] (step) {Next verification\\step};

  \draw[arrow] (api) -- (state);
  \draw[arrow] (state) -- (queue);
  \draw[arrow] (queue) -- (sched);
  \draw[arrow] (sched) -- (order);
  \draw[arrow] (order) -- (step);
  \draw[arrow] (step.north) |- ++(0,8mm) -| (api.south);
\end{tikzpicture}
