% Add this in the preamble if needed:
% \usetikzlibrary{arrows.meta,shapes.geometric}

\begin{tikzpicture}[
  >=Latex,
  font=\small,
  box/.style={
    draw,
    rounded corners=2pt,
    align=center,
    text width=25mm,
    minimum width=29mm,
    minimum height=13mm,
    inner sep=3pt,
    fill=orange!6
  },
  decision/.style={
    draw,
    diamond,
    aspect=2.1,
    align=center,
    text width=17mm,
    inner sep=1pt,
    fill=blue!5
  },
  endbox/.style={
    draw,
    rounded corners=2pt,
    align=center,
    text width=25mm,
    minimum width=29mm,
    minimum height=13mm,
    inner sep=3pt,
    fill=gray!15
  },
  arrow/.style={->, thick, shorten >=3pt, shorten <=3pt}
]

  % Main cycle
  \node[box] (api)   at (1.45,3.35) {(1) Standardized\\APIs};
  \node[box] (state) at (4.65,1.35) {(2) Task states\\and events};
  \node[box] (queue) at (4.65,-1.35) {(3) Ready\\queue};
  \node[box] (sched) at (1.45,-3.35) {(4) Deterministic\\scheduler};
  \node[box] (order) at (-2.50,-1.35) {(5) Task execution\\order};
  \node[box] (step)  at (-2.50,1.35) {(6) Next checking\\step};

  \node[decision] (more) at (-2.50,3.35) {More\\steps?};
  \node[endbox] (end) at (-6.25,3.35) {(7) Verification\\path ends};

  % Normal flow
  \draw[arrow] (api.east) -- (state.west);
  \draw[arrow] (state.south) -- (queue.north);
  \draw[arrow] (queue.west) -- (sched.east);
  \draw[arrow] (sched.west) -- (order.east);
  \draw[arrow] (order.north) -- (step.south);

  % Decision flow
  \draw[arrow] (step.north) -- (more.south);

  \draw[arrow] (more.east) --
    node[midway, above, fill=white, inner sep=1pt] {Yes}
    (api.west);

  \draw[arrow] (more.west) --
    node[midway, above, fill=white, inner sep=1pt] {No}
    (end.east);

\end{tikzpicture}