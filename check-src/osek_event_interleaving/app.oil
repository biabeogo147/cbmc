TASK controller {
  SCHEDULE = FULL;
  PRIORITY = 2;
  TYPE = EXTENDED;
  EVENT_MASK = 0x1;
  AUTOSTART = TRUE;
};

TASK worker {
  SCHEDULE = FULL;
  PRIORITY = 3;
  TYPE = BASIC;
  AUTOSTART = FALSE;
};

